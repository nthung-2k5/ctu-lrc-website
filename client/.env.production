# Environment Configuration
VITE_ENV=production

# Note: In production, use a reverse proxy like Nginx to handle API routing
# The client will still use relative paths (/api/...) which should be configured
# in your production server to forward to the backend API
